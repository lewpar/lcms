# Generated by LCMS — do not edit manually.
# Update via CMS Settings (gear icon on the site selector page).

# Each site is served at https://lcms-sites.cyberbilby.com/<site-slug>/
server {
    listen 80;
    server_name lcms-sites.cyberbilby.com;

    root /home/lewis/lcms/output;

    location / {
        try_files $uri $uri/ =404;
    }

    location ~ /\. {
        deny all;
    }
}
