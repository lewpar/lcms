# LCMS environment variables
# Copy this file to .env.local and adjust as needed.

# Override the root directory where LCMS stores its data (content/, output/, docs/).
# Defaults to the current working directory (process.cwd()) when not set.
# LCMS_DATA_DIR=/var/lib/lcms
